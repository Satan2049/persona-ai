[project]
name = "persona-ai-backend"
version = "1.3.2"
description = "Persona AI FastAPI backend and RAG services"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I"]
